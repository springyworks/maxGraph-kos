{
  "name": "@maxgraph/ts-example-built-with-vite",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --version && tsc && vite build --base ./",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "~4.4.12"
  }
}
