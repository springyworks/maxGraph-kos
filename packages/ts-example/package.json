{
  "name": "@maxgraph/ts-example-built-with-vite",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --version && tsc && vite build --base ./",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.3.3",
    "vite": "~5.0.12"
  }
}
